echo "🔎 커밋 메시지 포맷을 확인합니다..."

pnpm exec commitlint --edit "$1" || {
  echo "⚠️  커밋 형식이 올바르지 않습니다. 커밋이 중단됩니다."
  echo "참고: '✨ Feat: subject' 형식을 따라주세요. (예: ✨ Feat: 새로운 기능 추가)"
  exit 1
}

echo "✅ 커밋 메시지 포맷 확인을 완료했습니다. 커밋을 진행합니다."