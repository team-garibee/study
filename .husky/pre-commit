echo "🔎 파일/폴더 네이밍 규칙 검사 (ls-lint)..."
pnpm exec ls-lint || {
  echo "⚠️ ls-lint 실패: 파일/폴더 네이밍 규칙을 확인해주세요."
  exit 1
}

echo "🔎 staged 파일에 대한 Lint 검사..."
pnpm exec lint-staged || {
  echo "⚠️ Lint 검사에서 오류가 발생했습니다. 커밋이 중단됩니다."
  exit 1
}

echo "✅ Lint 검사가 완료되었습니다. 커밋을 진행합니다."
