ls:
  # 모든 폴더는 kebab-case
  .dir: kebabcase

  # 기본 파일 규칙 (camelCase)
  .ts: camelcase
  .js: camelcase

  # React 컴포넌트는 PascalCase
  packages/ui:
    .tsx: pascalcase

  packages/icons:
    .tsx: pascalcase

ignore:
  - node_modules
  - '**/node_modules'
  - dist
  - build
  - coverage
  - .turbo
  - .git
  - .vscode
